[build]
  command = "npm run build"
  publish = "dist/ng-expense-tracker/browser"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_ENV = "production"
